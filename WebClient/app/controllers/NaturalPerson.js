/*
 * Контроллер - физическое лицо 
 * 
 */
'use strict';

app.controller('NaturalPersonController', function ($scope, $http, $rootScope, $routeParams,
    naturalPersonsService) {

    // Мне нравится идея складывать код инициализации в один метод таким образом
    init();

    function init() {
        // id_naturalperson
       
    }
});
